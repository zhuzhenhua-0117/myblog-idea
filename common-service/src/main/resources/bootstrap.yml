server:
  port: 8051

spring:
  application:
    name: service
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: "*"