server:
  port: 8011

spring:
  application:
    name: admin
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: "*"